{{- if and .Values.audit.testWebhookReceiver.enabled .Values.audit.testWebhookReceiver.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "apiservice-audit-proxy.testWebhookReceiver.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "apiservice-audit-proxy.testWebhookReceiver.labels" . | nindent 4 }}
  {{- with .Values.audit.testWebhookReceiver.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.audit.testWebhookReceiver.ingress.className }}
  ingressClassName: {{ .Values.audit.testWebhookReceiver.ingress.className }}
  {{- end }}
  rules:
    - {{- if .Values.audit.testWebhookReceiver.ingress.host }}
      host: {{ .Values.audit.testWebhookReceiver.ingress.host | quote }}
      {{- end }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "apiservice-audit-proxy.testWebhookReceiver.fullname" . }}
                port:
                  name: http
{{- end }}
