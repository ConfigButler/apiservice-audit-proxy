{{- if .Values.backend.testApiserver.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "apiservice-audit-proxy.testApiserver.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "apiservice-audit-proxy.testApiserver.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.backend.testApiserver.replicaCount }}
  selector:
    matchLabels:
      {{- include "apiservice-audit-proxy.testApiserver.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.backend.testApiserver.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "apiservice-audit-proxy.testApiserver.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "apiservice-audit-proxy.testApiserver.serviceAccountName" . }}
      {{- with .Values.backend.testApiserver.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: wardle-server
          image: "{{ .Values.backend.testApiserver.image.repository }}:{{ .Values.backend.testApiserver.image.tag }}"
          imagePullPolicy: {{ .Values.backend.testApiserver.image.pullPolicy }}
          {{- with .Values.backend.testApiserver.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          args:
            - --etcd-servers=http://localhost:2379
            - --client-ca-file=/etc/wardle/proxy-client-ca/ca.crt
            {{- if .Values.backend.testApiserver.backendServingCert.enabled }}
            - --tls-cert-file=/etc/wardle/serving-cert/tls.crt
            - --tls-private-key-file=/etc/wardle/serving-cert/tls.key
            {{- end }}
          ports:
            - name: https
              containerPort: 443
              protocol: TCP
          resources:
            {{- toYaml .Values.backend.testApiserver.resources | nindent 12 }}
          volumeMounts:
            - mountPath: /etc/wardle/proxy-client-ca
              name: proxy-client-ca
              readOnly: true
            {{- if .Values.backend.testApiserver.backendServingCert.enabled }}
            - mountPath: /etc/wardle/serving-cert
              name: serving-cert
              readOnly: true
            {{- end }}
        - name: etcd
          image: "{{ .Values.backend.testApiserver.etcd.image.repository }}:{{ .Values.backend.testApiserver.etcd.image.tag }}"
          imagePullPolicy: {{ .Values.backend.testApiserver.etcd.image.pullPolicy }}
          resources:
            {{- toYaml .Values.backend.testApiserver.etcdResources | nindent 12 }}
      volumes:
        - name: proxy-client-ca
          secret:
            secretName: {{ include "apiservice-audit-proxy.testApiserver.clientCASecretName" . }}
        {{- if .Values.backend.testApiserver.backendServingCert.enabled }}
        - name: serving-cert
          secret:
            secretName: {{ include "apiservice-audit-proxy.testApiserver.servingCertSecretName" . }}
        {{- end }}
      {{- with .Values.backend.testApiserver.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.backend.testApiserver.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.backend.testApiserver.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
