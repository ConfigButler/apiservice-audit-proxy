apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared e2e services only. The sample-apiserver backend is Helm-managed through
# test/e2e/values/proxy-cert-manager*.yaml.
resources:
  - prometheus/instance.yaml
  - prometheus/prometheus-rbac.yaml
  - prometheus/servicemonitor-traefik.yaml
